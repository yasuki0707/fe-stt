apiVersion: v1
kind: Service
metadata:
  labels:
    app: stt-be
    app.kubernetes.io/instance: stt-be
    app.kubernetes.io/name: stt-be
  name: stt-be
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 3000
  selector:
    app.kubernetes.io/instance: stt-be
    app.kubernetes.io/name: stt-be
  sessionAffinity: None
  type: ClusterIP
